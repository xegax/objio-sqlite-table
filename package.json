{
  "name": "objio-sqlite-table",
  "version": "0.0.4",
  "description": "",
  "main": "index.js",
  "repository": "git@github.com:xegax/objio-sqlite-table.git",
  "scripts": {
    "prepare": "npm run clean && tsc -p tsconfig.publish.json -outDir .",
    "postpublish": "npm run clean",
    "dist": "tsc",
    "dev": "npm run clean-build && tsc -p tsconfig.publish.json -outDir build && shx cp package.json build",
    "clean": "npm run clean-src && npm run clean-build",
    "clean-src": "shx rm -rf src/**/*.js src/**/*.js.map src/**/*.d.ts *.d.ts *.js.map *.js",
    "clean-build": "shx rm -rf build/**/*"
  },
  "author": "xega",
  "license": "ISC",
  "devDependencies": {
    "@types/sqlite3": "^3.1.3"
  },
  "dependencies": {
    "objio": "^0.0.24",
    "objio-object": "^0.0.1",
    "sqlite3": "^4.0.2"
  }
}
